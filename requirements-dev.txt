# Development and CI dependencies for the iPermit monorepo.
#
# Application/runtime dependencies belong to each service/package; this file is
# the shared tooling + test stack used by pre-commit (T01-02/T01-12) and CI
# (T01-07). Pinned where version drift would change check results.
ruff==0.15.8
pytest>=8,<9

# Schema, rules, and persistence tooling (EPIC-00 specs -> EPIC-02 data layer).
jsonschema>=4.20
pyyaml>=6
pydantic>=2.6
sqlalchemy>=2.0
alembic>=1.13

# GIS spatial foundation + shapefile/KMZ ingestion (EPIC-05, T05-01/T05-02).
# Pure-pip only: shapely bundles GEOS and pyshp is pure Python, so detection and
# ingestion are unit-testable in CI without system GDAL/PostGIS. PostGIS is the
# production store (see services/gis-engine/README.md), not a test dependency.
shapely>=2.0
pyshp>=2.3
