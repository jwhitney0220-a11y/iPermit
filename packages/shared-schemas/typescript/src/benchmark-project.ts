// DO NOT EDIT - generated by scripts/schema/generate.py

* This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Declarative benchmark project used for regression testing, edge-case validation, and audit replay of the deterministic rules engine. See docs/specs/benchmarks.md (T00-06). Defers rule object shape to T00-01, explanation record shape to T00-05, temporal selection to T00-03, regression framework to T04-02, and real Texas regulatory data to T02-06.
 */
export type BenchmarkProject = {
  [k: string]: unknown;
} & {
  /**
   * Stable kebab-case ID, globally unique, never reused or renamed.
   */
  benchmark_id: string;
  /**
   * Semver of the benchmark definition itself. Distinct from ruleset_version. MAJOR bump on any expected-output change.
   */
  benchmark_version: string;
  /**
   * One of the six required benchmark categories from AGENTS.md.
   */
  category:
    | "simple_linear"
    | "multi_county"
    | "federal_nexus"
    | "conflicting_jurisdiction"
    | "temporal_versioning"
    | "known_unknown";
  /**
   * One to two sentence summary of what edge case this benchmark exercises.
   */
  description: string;
  /**
   * Mirror of the consultant intake form (T06-02) plus GIS auto-detection results (T05-03/T05-04). Field registry is owned by T02-02; this schema allows additional properties so benchmarks can exercise future inputs without a schema bump.
   */
  project_inputs: {
    /**
     * Project type token. Registry owned by T02-02.
     */
    project_type: string;
    /**
     * Two-letter state code. MVP scope is TX only.
     */
    state: string;
    /**
     * @minItems 0
     */
    counties?: string[];
    municipalities?: string[];
    etjs?: string[];
    linear?: boolean;
    length_miles?: number;
    acreage?: number;
    stream_crossings_count?: number | null;
    has_wetlands?: boolean | null;
    txdot_involvement?: boolean;
    federal_nexus?: boolean | null;
    /**
     * Right-of-way type token, e.g. private_easement, public_row, txdot_row.
     */
    row_type?: string;
    shapefile_uploaded?: boolean;
    /**
     * Detected geometry.* values. Shape owned by T05-04; allows additional properties.
     */
    gis_overlays?: {
      [k: string]: unknown;
    };
    /**
     * Inputs known to be missing or incomplete at evaluation time.
     */
    partial_data_flags?: string[];
    [k: string]: unknown;
  };
  ruleset_version: {
    /**
     * Hash of the effective ruleset snapshot used. Real hashes are sha256:... per T00-05; pre-T02-06 stubs use placeholder:... so design-time benchmarks remain schema-valid.
     */
    content_hash: string;
    commit_sha?: string;
    snapshot_label?: string;
  };
  /**
   * Date the engine should treat as 'today' when evaluating this benchmark. Drives temporal rule selection (T00-03).
   */
  evaluation_date: string;
  expected_outputs: {
    /**
     * Permits the engine is expected to emit. Empty array is valid when the benchmark asserts that no permits should fire.
     *
     * @minItems 0
     */
    permits: {
      name: string;
      code?: string;
      agency: string;
      /**
       * Optional reference to the rule_id expected to fire. Becomes resolvable post-T02-06.
       */
      rule_id?: string;
    }[];
    /**
     * Free-text sequencing assertions. Detailed sequencing checks are owned by T00-04 / T03-04.
     */
    sequencing_notes?: string[];
  };
  /**
   * Expected confidence tier per permit. Order should match expected_outputs.permits.
   */
  expected_confidence: {
    permit_name: string;
    tier: 1 | 2 | 3;
  }[];
  /**
   * Known unknowns the engine should surface. Compared as a set (order-independent) at regression time by T04-02.
   */
  expected_known_unknowns: string[];
  /**
   * High-level expected explanation assertions, one per fired permit. Detailed explanation record shape is owned by T00-05 (see permit-explanation.schema.json); benchmarks assert only the load-bearing fields here.
   */
  expected_explanations: {
    permit_name: string;
    /**
     * Plain-language assertion of WHY this permit is expected to fire. Compared semantically by analysts during review; T04-02 does keyword-presence checks.
     */
    trigger_summary: string;
    expected_rule_id?: string;
    expected_jurisdiction_levels?: (
      | "federal"
      | "state"
      | "county"
      | "municipality"
      | "etj"
      | "utility_district"
      | "drainage_district"
      | "river_authority"
      | "special"
    )[];
  }[];
  /**
   * Mandatory advisory strings expected to appear. Used to verify AGENTS.md Liability Strategy compliance.
   */
  expected_advisories?: string[];
  /**
   * Required for temporal_versioning benchmarks. Captures the specific rule version(s) the benchmark asserts behavior against, so that future rule edits do not silently change historical replay results.
   */
  historical_rule_version?: {
    /**
     * @minItems 1
     */
    rules: [string, ...string[]];
    /**
     * Free-text label, e.g. 'pre-2025-rule-change' or 'post-2025-rule-change'.
     */
    scenario_label?: string;
  };
  provenance: {
    author: string;
    created_at: string;
    reviewer?: string;
    reviewer_notes?: string;
    /**
     * Optional pointer to the real or anonymized historical project that inspired this benchmark.
     */
    source_project_reference?: string;
  };
  /**
   * Benchmarks are immutable by definition (AGENTS.md). The only acceptable value is true. Material changes require a NEW benchmark_id, not an edit.
   */
  immutable: true;
  /**
   * Free-text commentary on what this benchmark is intended to exercise.
   */
  notes?: string;
  tags?: string[];
};
