# 03 — System Architecture

**Status:** Placeholder
**Owner ticket(s):** T01-01 (repository & CI/CD bootstrap), T03-02 (rule evaluation engine)

---

## What This Section Will Cover

A narrative and accompanying diagram describing the end-to-end iPermit architecture: how a consultant's intake submission flows through GIS auto-detection, the rule evaluator, the conflict resolver, the sequencer, the explainability layer, and finally into the permit matrix output. It will name every service boundary, every data store, every async job, and every external integration.

## [Pending: filled after EPIC-01 + EPIC-03 ship. Owner: T01-01 + T03-02]

This section is intentionally left blank. The architecture cannot be drawn until:

- The repository scaffold exists (T01-05).
- The CI/CD environment is configured (T01-01, T01-07).
- The API contract between services is defined (T01-15).
- The rules engine has a working evaluator (T03-02) — without an evaluator, there is no run-time architecture to describe.
- The spatial DB infrastructure is planned and stood up (T01-16 → T05-01).

Writing this section before those tickets land would produce speculative diagrams that the implementation would then diverge from. We would rather have an authoritative section after the first end-to-end path runs than a fictional one before.

## What to Consult Instead

Until this section is authored, use these sources for architectural questions:

- [`AGENTS.md`](../../AGENTS.md) — read in full. The *Rules Engine Architecture*, *AI Strategy*, *GIS Strategy*, and *Repository Strategy* sections together describe the intended shape of the system.
- [`docs/roadmap.md`](../roadmap.md) — the EPIC structure (EPIC-01 through EPIC-09) is the closest thing to a layered architecture description that exists today. Each EPIC corresponds roughly to one architectural layer.
- [`docs/specs/rule-object.md`](../specs/rule-object.md) — the rule object is the central data structure the architecture is built around. Understanding the rule object explains most of the engine's shape.
- [`docs/engineering-handbook/02-repository-strategy.md`](./02-repository-strategy.md) — the directory layout is a deliberate reflection of the intended service boundaries.

## How to Help

If you are working on T01-01 or T03-02 and need to start sketching the architecture, open a draft PR that adds a `diagrams/` folder under this directory and put working-draft diagrams there. Do not edit this file's main body until the owning ticket is ready to ship the section in full.

When this section is authored, the author should remove the *Pending* line, update the *Status* to *Authored*, and update the table of contents in the handbook [README](./README.md).
